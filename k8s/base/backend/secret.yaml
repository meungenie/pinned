# 이 파일은 git에 올리지 않음 (.gitignore에 추가됨)
# 실제 배포 시 GitHub Actions Secret에서 자동 생성
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: pinned
type: Opaque
stringData:
  DB_USER: "pinned_user"
  DB_PASSWORD: "REPLACE_ME"
